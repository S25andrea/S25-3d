var animation = bodymovin.loadAnimation({
    container: document.getElementById('frontContainer'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: "./images/transparent/front.json" // lottie file path
  })

  var animation = bodymovin.loadAnimation({
    container: document.getElementById('leftContainer'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: "./images/transparent/left.json" // lottie file path
  })

  var animation = bodymovin.loadAnimation({
    container: document.getElementById('rightContainer'),
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: "./images/transparent/right.json" // lottie file path
  })